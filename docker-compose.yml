version: '3.8'

services:
  it-signer:
    build: .
    container_name: it_handover_signer
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./pending:/app/pending
      - ./signed:/app/signed
      - ./config.json:/app/config.json
    environment:
      - PORT=5000
